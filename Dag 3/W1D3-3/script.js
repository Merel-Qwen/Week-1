/*const tellFortune = function(job, city, partner, kids) {
  console.log(
    "You will be a " +
      job +
      " in " +
      city +
      " , and marrie to " +
      partner +
      " with " +
      kids +
      " kids"
  );
};

tellFortune("programmer", "Amsterdam", "pietje", 3);
const calculateDogAge = function(humanYears) {
  const dogYears = 7 * humanYears;
  console.log("Your doggie is " + dogYears + " years old in dog years!");
};

calculateDogAge(2);

const calculateSupply = function(age, amountPerDay) {
  const maxAge = 100;
  const totalAmount = Math.round((maxAge - age) * amountPerDay * 365);
  console.log(totalAmount);
  console.log(
    "You will need " +
      totalAmount +
      " to last you until the ripe old age of " +
      maxAge
  );
};

calculateSupply(50, 1.333); 

for (let i = 0; i <= 20; i++) {
  if (i % 2 === 0) {
    console.log(i + " is even");
  } else {
    console.log(i + " is odd");
  }
} 

for (let i = 0; i <= 10; i++) {
  let multiplier = 9;

  let result = multiplier * i;

  console.log(multiplier + " * " + i + " = " + result); */

const colors = ["yellow", "bleu", "red", "orange"];
//console.log(colors[2]);
//console.log(colors[0])

//let i = 0;
//while (i < colors.length) {
// console.log("This is the color " + colors[i]);
//  i++;
//}

for (let i = 0; i <= 10; i++) {
  let multiplier = 9;

  let result = multiplier * i;

  console.log(multiplier + " * " + i + " = " + result);
}
